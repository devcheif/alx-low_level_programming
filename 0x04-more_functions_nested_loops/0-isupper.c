#include "main.h"
#include <stdio.h>

/**
 * main - Entry point
 * _isupper - evalue if letter is uppercase.
 *@c: print int
 * Return: Always 0 (Success).
 */
int main(void)

{
int _isupper(int c);

{
char c;

else if (c >= 'a' && c <= 'z')
{
printf("%c: %d\n", c, _isupper(c));
}

else if (c >= 'a' && c <= 'z')
{
printf("%c: %d\n", c, _isupper(c));
}
}

return (0);
}
