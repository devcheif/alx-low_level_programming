#include "main.h"
#include <stdio.h>

/**
 * main - Entry point
 * _isupper - evalue if letter is uppercase.
 *@c: print int
 * Return: Always 0 (Success).
 */
int main(void)

{
int _isupper(int c);

{
char c;
if (c = 'A' ||  c <= 'Z')
{
return (1);
printf("%c : 1", c);
}

else
{
return (0);
}
}

return (0);
}
