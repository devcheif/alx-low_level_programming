#include "main.h"
#include <stdio.h>

/**
 * main - Entry point
 * _isupper - evalue if letter is uppercase.
 *
 * Return: Always 0 (Success).
 */
int main(void)

{
int _isupper(int c);

{
char c;

if (c >= 'a' && c <= 'z')
{
printf("%c: %d\n", c, 1);
}

else if (c >= 'a' && c <= 'z')
{
printf("%c: %d\n", c, 0);
}
}

return (0);
}
